---
all:
  hosts:
    malatesta:
      ansible_host: 127.0.0.1
      ansible_user: christophe_work
      ansible_password: "{{ user_pwd }}"
      ansible_become_password: "{{ user_pwd }}"
      ansible_python_interpreter: /usr/bin/python
    ccs-online-catercare-app:
      ansible_user: christophe_bahin_catercare_com_a
      ansible_connection: google.cloud.iap
      ansible_gcloud_project: erudite-skill-379500
      ansible_gcloud_zone: australia-southeast2-c
      ansible_ssh_args: "-A"
  children:
       centos:
          hosts:
       debian:
          hosts:
       ubuntu:
          hosts:
            ccs-online-catercare-app:
       fedora:
         children:
           gnome:
             hosts:
               malatesta:
     # popos:
     #   children:
     #     cosmic:
     #       hosts:
     #         malatesta:
