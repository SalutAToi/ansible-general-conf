---
all:
  hosts:
    malatesta:
      ansible_host: 127.0.0.1
      ansible_user: christophe_work
      ansible_password: "{{ user_pwd }}"
      ansible_become_password: "{{ user_pwd }}"
      ansible_python_interpreter: /usr/bin/python
    gcp-vm-example:
      ansible_user: user
      ansible_connection: google.cloud.iap
      ansible_gcloud_project: gpc-project
      ansible_gcloud_zone: australia-southeast1-a
      ansible_ssh_args: "-A"
  children:
       centos:
        hosts:
        gcp-vm-example::
       fedora:
         children:
           gnome:
             hosts:
               malatesta:
     # popos:
     #   children:
     #     cosmic:
     #       hosts:
     #         malatesta:
